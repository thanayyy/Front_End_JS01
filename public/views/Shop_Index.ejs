<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>

    ดูข้อมูลDatabase ทั้งหมด
    <a href="/Employee_Page">พนักงาน</a>
    <a href="/Item_Page">สินค้า</a>
    <a href="/Ticket">ตั๋ว</a>
    <a href="/Customer_Page">ลูกค้า</a>
    <br>

    <% var Id_Customer = 0%>
        <% for(let i = 0; i < Customers.length;i++) {%>

            <% if(Name == Customers[i].Name ){%>
                ชื่อ :
                <%= Name%>
                    <br> รหัสลูกค้า :
                    <%= Customers[i].Customer_Id%>
                        <% Id_Customer = Customers[i].Customer_Id %>
                            <br>
                            <a href="/Pawn_Product/<%=Name%>/<%=Customers[i].Customer_Id%>">จำนำของ</a>
                            <% break %>
                                <%}%>
                                    <%}%>

                                        <a href="/Record_Pawn/<%=Id_Customer%>">ดูประวัติการจำนำ</a>

                                        <a href="/">Log Out</a>

</body>

</html>